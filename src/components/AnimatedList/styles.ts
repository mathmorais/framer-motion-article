import styled from "@emotion/styled";
import { motion } from "framer-motion";

export const StyledAnimatedList = styled(motion.ul)`
  display: flex;
  flex-direction: column;
  list-style: none;
  gap: 16px;
`;
